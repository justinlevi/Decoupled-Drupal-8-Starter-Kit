# Valid values are:
#   install - Installs Drupal from scratch.
#   sync - Uses `blt sync` to pull a remote db from drush.aliases.remote.
#   import - Imports a .sql file from setup.dump-file.
 setup.strategy: install
# setup.dump-file: test-dump.sql
